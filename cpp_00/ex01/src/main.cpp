#include "phonebook_class.hpp";
using namespace std;

int main (int ac, char **av)
{
	cout << "input ADD, SEARCH or EXIT";
	while (1)
	{
		if(ac == 2)
		{
			if(av[1] == "EXIT")
				exit (EXIT_SUCCESS);
			else if(av[1] == "ADD")
			{
				
			}

		} 
	}
}